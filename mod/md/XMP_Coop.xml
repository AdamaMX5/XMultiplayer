<?xml version="1.0" encoding="utf-8"?>
<mdscript name="XMP_Coop" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../../../libraries/md.xsd">
  <!--
    XMultiplayer Coop (PlanMod.md Phase 2, "Coop im Universum des Hosts"):
    Milestone C1 (static sector mirror) and, further below, Milestone C2
    (host/guest ship mirroring, reusing Arena code as-is). Everything in this
    file is PLAUSIBLE, not VERIFIED, same status as XMP_Telemetry.xml/
    XMP_Arena.xml (A1-A5) - no X4 installation is available in this
    development environment. See docs/C1-messprotokoll.md and
    docs/C2-messprotokoll.md for the full assumption lists.

    SCOPE OF C1: when ANY session member's `session` `join` is received (by
    every OTHER member, via the existing dispatch this file plugs into -
    XMP_Arena.xml's XMP_Arena_Dispatch), each recipient walks ITS OWN CURRENT
    player.sector once and exports every station/gate/asteroid field/region it
    finds as a one-shot burst of `sector_object` messages, bracketed by
    `sector_mirror` begin/end. There is deliberately NO host/guest distinction
    anywhere in this protocol or the server (see protocol/src/messages.ts's
    SectorObjectMessage doc comment) - this file is fully symmetric, exactly
    like XMP_Arena.xml's spawn/despawn handling. A member whose current sector
    has nothing of interest (the empty Arena sector, or an unpopulated sector)
    simply exports zero objects (`sector_mirror` begin with objectCount=0,
    immediately followed by end) - harmless, not a special case anywhere in
    this file.

    DELIBERATELY NOT ATTEMPTED IN C1 (documented simplifications, not oversights,
    see docs/C1-messprotokoll.md "Nächste Schritte" for the follow-up plan):
      - No new "Coop waiting room" gamestart ships with this milestone. PlanMod.md's
        Phase 2 architecture principle ("Gast startet in leerem Custom-Universum")
        is not yet implemented as shippable content - for C1, whichever player
        wants to see the OTHER's sector mirrored simply needs to be in a sector
        with little/nothing of their own to avoid a confusing double-mirror
        (both directions run identically). Revisit once a dedicated empty
        gamestart is worth the added content-authoring surface (same shape as
        the Arena's four gamestart variants, libraries/{clusters,sectors,zones,
        galaxy,gamestarts}.xml).
      - Guest-side proxies are NOT differentiated by objectType yet (station vs.
        gate vs. asteroid field vs. region all use the exact same placeholder
        creation call, XMP_Coop_HandleSectorObject below) - objectType is
        carried on the wire and tracked, but only used for logging in this
        milestone. A real per-type visual treatment is future work.
      - No re-export throttling: if a THIRD member joins later, every
        already-present member (including the guest who received the FIRST
        export) re-runs XMP_Coop_ExportSector and re-sends the same objects.
        XMP_Coop_HandleSectorObject's `not $XMP.SectorProxies.{objectId}.exists`
        guard (mirroring XMP_Arena_HandleSpawn's own idempotency check) makes a
        redundant re-receive a harmless no-op, so this is accepted rather than
        solved with e.g. server-side caching (unlike A2's spawn replay-to-late-
        joiners, sessionManager.ts, this deliberately has no server-side
        replay/cache for sector_object - see docs/C1-messprotokoll.md).
      - No despawn/teardown path for sector proxies: PlanMod.md's C1 spec calls
        this a "statisches" (static), one-time transfer with no further sync,
        so there is currently no message type or handler that ever removes a
        $XMP.SectorProxies entry. A session leave/leave-sector event does NOT
        clean these up (contrast with XMP_Arena_OnExitSector's real ship
        despawn) - accepted for V1, revisit once C5 ("Sektorwechsel") exists
        and a guest's mirrored world needs to be torn down and rebuilt for a
        new sector.
      - Not cached in the agent's knownSpawns/replayed on a pipe reconnect
        (agent/src/index.ts) - a local X4 client restart mid-Coop-session loses
        the mirrored static objects until the next full session (re)join sends
        a fresh `session` join and re-triggers export. See
        docs/C1-messprotokoll.md.

    THE SINGLE HIGHEST-RISK ITEM IN THIS FILE: XMP_Coop_FindObjectsOfClass below,
    the sector-object enumeration itself. Explore of this codebase (prior to C1)
    found NO existing pattern anywhere in mod/ for walking "every object of a
    given class in a sector" - A1-A5 only ever read the PLAYER's OWN ship
    (player.entity.*) or, since A5, player.sector.macro.name as a single scalar
    comparison, never a collection. XMP_Coop_FindObjectsOfClass assumes a generic
    `find_object` action exists, taking `class` ('station'/'gate'/'asteroid'/
    'region') and `sector` attributes plus `multiple="true"`, and that its result
    can be iterated with `<do_all exact="$Result" counter="$XMP.CoopIndex">`
    reading `$XMP.CoopIndex.entry` per iteration - the same "one action,
    parameterized by class" shape used for id/name/position/rotation access
    below, chosen specifically so that IF this assumption is wrong, fixing it
    means correcting ONE cue (four call sites, one implementation), not four
    independently-wrong ones. If `find_object` turns out not to exist, the
    fallback is per-class actions (`find_station`, `find_gate`, `find_asteroid`,
    `find_region`, all documented as existing in various community MD
    references) with the SAME iteration shape - XMP_Coop_ExportSector's four
    call sites would need their single `<include_interrupt_actions>` target
    changed, nothing else in this file.

    Further assumptions, in descending risk order:
      - `.id`/`.macro.name`/`.position`/`.quaternion` are accessible on whatever
        object reference `$XMP.CoopIndex.entry` yields, mirroring the exact
        accessor style already used (and unconfirmed, same risk category) for
        player.entity in XMP_Arena.xml/XMP_Telemetry.xml since A1 - in
        particular `.quaternion` (not `.rotation`), matching
        XMP_Telemetry.xml's `player.entity.quaternion` exactly (an internal
        review round caught an earlier draft of this file using `.rotation`
        here, inconsistent with that established accessor).
      - `create_station` is the action used to place a guest-side placeholder for
        EVERY sector_object type, regardless of its real objectType (station,
        gate, asteroid field, or region) - see XMP_Coop_HandleSectorObject
        below. Assumed signature: `<create_station name="$Var" macro="..."
        sector="player.sector" position="..." rotation="..." owner="ownerless"
        />`. Unconfirmed; if station placement in particular fails (unlike a
        ship, a station may have per-macro construction/module requirements
        vanilla `create_station` calls in mission scripts are known to satisfy
        automatically - unverified here), the whole guest-side half of this
        milestone needs a different placement action.
      - $XMP.CoopPlaceholderMacro ('xmp_coop_placeholder_macro', set in
        XMP_Coop_HandleSectorObject below): a PURE PLACEHOLDER string, following
        the same "documented, not verified against the game's own library
        files" caveat as A5's three region macro placeholders
        (docs/A5-messprotokoll.md section 9). Must be replaced with a real,
        confirmed-to-exist macro (a small, cheap, faction-neutral object,
        e.g. a navigation beacon or similar low-footprint vanilla macro)
        before this can be tested in-game at all.
      - `player.sector` is reused here exactly as XMP_Arena_PresenceLoop (A5)
        already assumes it (`.macro.name`); C1 additionally assumes plain
        `player.sector` (without `.macro.name`) is itself a valid `sector`
        attribute value for `find_object`/`create_station`, not just readable
        for its macro name.

    Milestone C2 ("Host-Schiff als dynamisches Objekt = Arena-Code
    wiederverwendet"): mirrors each session member's own REAL ship to the
    others, reusing XMP_Arena_HandleSpawn/HandleDespawn/HandleStateUpdate/
    aiscripts/XMP.ProxyPilot.xml UNCHANGED - they already handle any `spawn`/
    `despawn`/`state_update` generically regardless of what triggered it
    (Arena-sector presence, or C2's own trigger below), and
    XMP_Telemetry_Tick already streams `state_update` unconditionally
    (player.entity.exists/isclass.ship only, no Arena/Coop gating anywhere in
    that file) - so C2 needed ZERO changes to any of that reused code, only a
    new trigger for the ONE piece Arena-presence used to provide: the initial
    `spawn` announcing our own ship.

    THE CORE PROBLEM C2 HAD TO SOLVE: unlike Arena (whose fixed sector-entry
    trigger, XMP_Arena_OnEnterSector, unconditionally fires `session join` +
    `spawn` together), Coop sessions are joined via the agent's explicit
    "session" CLI flag/XMP_SESSION override (A5's "operator override" mechanism)
    - that join is sent straight from the agent to the relay server over the
    WebSocket and NEVER otherwise reaches MD, so nothing in this file could
    previously know "I am now in an active session, announce my ship." Worse,
    C1's own `session`-`join` dispatch branch (XMP_Coop_HandleSessionJoin)
    only ever fires for a join RECEIVED from ANOTHER member (broadcast()
    excludes the sender) - so relying on that alone would mean the FIRST
    member to join a session (nobody there yet to receive their join) would
    never announce their own ship at all, only whoever joins SECOND would (by
    reacting to the first member's... except the first member's join was never
    seen by anyone either, symmetric dead end both ways).

    THE FIX (agent/src/index.ts, agent/src/sessionState.ts, not this file):
    the agent now loops its own explicit-session join line back into the LOCAL
    pipe (in addition to sending it to the relay as before), exactly once per
    WS connection (`coopSelfAnnounceDeliveredToPipe`), covering both connection
    orderings (game connects to the pipe before or after the WS join was sent)
    via a combination of an immediate attempt in the WS `onOpen` handler and a
    replay in the pipe's `onClientConnected` handler using
    `SessionState.lastJoinLine()` (new). This is REAL, VERIFIED behavior
    (agent/tests/relayToPipe.e2e.test.ts spawns the actual agent process end to
    end and asserts the loopback happens exactly once, with the right
    sessionCode/playerName, and does NOT repeat on a later pipe reconnect) -
    unlike everything MD-side in this file, which stays PLAUSIBLE-only.

    THIS FILE'S role, once that loopback exists: XMP_Coop_HandleSessionJoin
    (extended from C1) now distinguishes OUR OWN join (playerName ==
    player.name) from someone ELSE's. Someone else's join still only triggers
    the C1 sector export - our own ship's spawn does not need re-sending on
    every later join, since server/src/sessionManager.ts already replays any
    currently-known spawn (regardless of origin) to a newly joined member, the
    exact A2 mechanism Arena proxies have always relied on for the same
    purpose. Our OWN join does NOT announce our ship immediately/synchronously
    (an internal review round found and fixed a real bug in that first
    design, see XMP_Coop_HandleSessionJoin's own comment below for the full
    story) - it only marks $XMP.CoopPendingSelfAnnounce; a separate
    once-a-second poll (XMP_Coop_SelfAnnounceCheck) actually announces once
    the player's ship exists, decoupling "our join arrived" from "our ship is
    ready to describe," which are not guaranteed to become true at the same
    moment.

    DELIBERATELY NOT ATTEMPTED IN C2 (same "document, don't solve everything"
    posture as C1's own list above):
      - No explicit "leave Coop" despawn path while staying connected. A
        genuine WebSocket disconnect already despawns everything that client
        owned via the existing, fully generic A2-A5 disconnect handling
        (server.ts's handleDisconnect/broadcastDespawns, never Arena- or
        Coop-specific) - but there is no way to leave a Coop session
        deliberately without disconnecting the agent entirely, unlike Arena's
        sector-exit-triggered XMP_Arena_OnExitSector. Revisit alongside C5
        ("Sektorwechsel") if that turns out to matter in practice.
      - Combat between mirrored player ships is untouched/unconsidered: a
        Coop-announced ship proxy is invulnerable exactly like an Arena proxy
        (XMP_Arena_HandleSpawn's existing set_object_invulnerable, reused
        as-is), and hit-reporting/hp_state flow identically - meaning two
        Coop players COULD currently damage each other's mirrored ships via
        the exact same hit_report/hp_state path Arena PvP uses, since nothing
        in the protocol or server distinguishes "Arena combat" from "Coop
        session" (there is no such distinction anywhere, same as the
        host/guest non-distinction from C1). Whether that is desired for Coop
        is a product decision PlanMod.md does not explicitly make either way;
        noted here rather than silently assumed.

    Milestone C3 ("NPC-Bubble mit Interest Management"): XMP_Coop_BubbleCheck
    below periodically exports NPC ships near our own position as ordinary
    `spawn`/`state_update`/`despawn` messages (category="npc", C3's one
    protocol addition, protocol/src/messages.ts's SpawnMessage.category) -
    reusing XMP_Arena_HandleSpawn/HandleDespawn/HandleStateUpdate/
    aiscripts/XMP.ProxyPilot.xml COMPLETELY UNCHANGED, the same "Arena-Code
    wiederverwendet" principle C2 already established for player ships. See
    XMP_Coop_BubbleCheck's own comment for the detailed design and its
    assumptions (the single riskiest MD code in this file after C1's
    XMP_Coop_FindObjectsOfClass) and docs/C3-messprotokoll.md for the
    protocol/server/agent-side changes this milestone needed (a client's A4
    one-active-spawn cap now applies ONLY to category="player"; a SEPARATE,
    independent MAX_NPC_SPAWNS_PER_CLIENT budget applies to category="npc";
    SHIP_MACRO_WHITELIST is skipped entirely for "npc" spawns, since it was
    never meant to cover real NPC ship diversity).

    DELIBERATELY NOT ATTEMPTED IN C3 (same posture as C1/C2's own lists
    above):
      - Bubble center is OUR OWN ship's position, not "the other session
        member's position" as PlanMod.md's prose literally says ("Host
        exportiert NPC-Schiffe im Radius um den GAST"). In the common
        2-player "flying together" scenario these converge to nearly the
        same result; the alternative (centering on every OTHER member's own
        mirrored proxy position) would need a currently-nonexistent
        "enumerate all current session members" mechanism in MD (only a
        by-name lookup table exists, $XMP.ProxyObjectIdByOwner, A5) to
        generalize past exactly 2 players. Revisit once real multi-member
        Coop sessions (3+) are a target, or once C5 ("Sektorwechsel")'s own
        "Host und Gast in verschiedenen Sektoren" edge case forces the
        question anyway (PlanMod.md already flags that as an open decision
        point "nach C3-Performance-Daten").
      - Single-tier update rate for every tracked NPC (2s, tunable), not
        PlanMod's literal "volle Update-Rate für nahe, seltener für ferne"
        two-tier design - see XMP_Coop_BubbleTuningDefaults's own comment.
      - No real macro whitelist for NPC shipType (same posture as C1's
        station/gate/asteroid/region macro names) - length-capped
        (MAX_MACRO_NAME_LENGTH, applies to EVERY spawn.shipType since C3,
        protocol/src/parse.ts) and sanitized before reaching the pipe
        (agent/src/pipeSanitize.ts), but not whitelisted.
      - No explicit hit-relay/kill-attribution design for NPCs in C3 itself -
        PlanMod.md scoped that to C4 ("Hit-Relay auf NPCs"), now added below
        (XMP_Coop_BubbleCheck's new $XMP.CoopBubbleGameObjectByWireId tracking
        and the new XMP_Coop_ForgetBubbleNpc cue, consumed by
        mod/md/XMP_Arena.xml's XMP_Arena_HandleHpState's new third branch) and
        server-side (server/src/server.ts's broadcastKillFeed now names an NPC
        victim by its shipType instead of the exporting player's own name -
        see that function's own doc comment). See docs/C4-messprotokoll.md.

    Milestone C5 ("Sektorwechsel"): XMP_Coop_SectorChangeLoop/
    SectorChangeCheck below detect OUR OWN sector changing (e.g. a gate
    transit) and react with two local teardowns plus a broadcast -
    XMP_Coop_HandleSectorChange's own comment carries the full design. In
    short: destroy every currently-held static sector-mirror placeholder
    (finally closing the "no despawn/teardown path for sector proxies"
    limitation C1 explicitly deferred to this milestone), force-clear the
    NPC bubble immediately instead of waiting for its normal stale timeout
    (PlanMod.md's own "Bubble abreißen" wording, taken literally), then
    broadcast a new `session` `sector_change` action (protocol/src/
    messages.ts's SessionAction, C5's one protocol addition) so every OTHER
    session member re-exports their own current sector to us
    (XMP_Coop_HandleSessionJoin, extended to treat `sector_change` the same
    as someone else's `join` for sector-mirroring purposes).

    Resolves the "Host und Gast in verschiedenen Sektoren" edge case
    PlanMod.md flags for C5 - but not by building the two-bubbles-in-
    parallel machinery PlanMod's own prose speculates about. Because the NPC
    bubble (C3) already centers on EACH CLIENT'S OWN ship position and
    queries `player.sector` fresh every tick, independently per client, two
    session members already end up with two entirely independent, correctly
    self-scoped bubbles the moment they are in different sectors - nothing
    in this design assumed they'd be in the same one. The only actual gap
    was static-sector-mirror staleness after a move, which this milestone's
    teardown/re-export closes. The bubble-center generalization noted above
    (centering on every OTHER member's position instead of only our own, for
    3+-player sessions) remains open - a different, unrelated question from
    the different-sectors case, now resolved to be a non-issue.

    DELIBERATELY NOT ATTEMPTED IN C5 (same posture as C1-C4's own lists):
      - No "Ladebildschirm"/fade UI of any kind - PlanMod.md itself says
        "kein nahtloser Übergang nötig" (no seamless transition needed); the
        teardown is an instant, silent state reset, not a presented visual
        effect (X4's OWN gate-transit loading screen, unrelated to this
        mod, already provides the "moment" PlanMod.md's wording refers to).
      - Our own ship's proxy on other members' clients is NOT explicitly
        snap-corrected by any new C5 code - a gate transit is a huge
        position delta, already well past
        $XMP.DeadReckoningSnapThresholdMeters (500m, XMP_Arena_
        TuningDefaults), so aiscripts/XMP.ProxyPilot.xml's EXISTING Dead
        Reckoning snap-teleport fallback (A3) already handles it: nothing
        new was needed here, a genuinely reused mechanism rather than a
        deferred gap.
      - `sector_change`'s `sessionCode` field hardcodes the LAN default
        `"arena"`, inaccurate for a real Coop session's actual code - the
        SAME pre-existing limitation XMP_Arena_SetaCheck's seta_on/seta_off
        broadcasts already have (A5), not a new one, and harmless for the
        same reason (server/src/server.ts never reads `sessionCode` for
        anything but the "join" action). See docs/C5-messprotokoll.md.

    Milestone C6 ("Kommando-Relay", Docking only - PlanMod.md's own "bewusst
    inkrementell, sortiert nach Wert/Aufwand" framing, repair/resupply/trade/
    fleet-commands/missions explicitly deferred, developer decision): three
    new cues below - XMP_Coop_OnDock (guest-side detection, sends
    `dock_request`), XMP_Coop_HandleDockRequest (host-side validation +
    `dock_response`), XMP_Coop_HandleDockResponse (guest-side reaction) - plus
    two small additions to existing cues: XMP_Coop_FindObjectsOfClass now also
    keeps a PERSISTENT host-side table ($XMP.CoopExportedObjectByWireId,
    keyed by the same wire id C1 already reuses from the real object's own
    `.id`) of every real object we've ever exported, since C1 itself only
    ever kept a transient, export-then-discard list; XMP_Coop_HandleSectorObject
    now also keeps a reverse lookup ($XMP.SectorProxyWireIdByGameId, mirroring
    $XMP.ProxyWireIdByGameId's existing role for ship proxies, A4) so a local
    docking event's game-object reference can be turned back into the wire id
    a dock_request needs. XMP_Coop_HandleSectorChange (C5) was extended to
    also clear the new reverse-lookup entry when tearing down a stale mirror,
    same symmetric-cleanup rigor as everywhere else in this file.

    THE CORE DESIGN QUESTION C6 HAD TO SOLVE: every message type before this
    one was either broadcast to the whole session (state_update, spawn,
    sector_object, ...) or server-generated (hp_state). A docking interaction
    is inherently two-party (the requesting player and whichever member
    exported the target station) - broadcasting it to a THIRD, uninvolved
    member would be both wasteful and a mild information leak (who is docking
    where). server/src/server.ts's new `sendToMember` is the fix: point-to-
    point routing, keyed by a NEW ownership map for `sector_object`
    (`sectorObjectOwnerOf`, server/src/sessionManager.ts) for `dock_request`,
    and by the ALREADY-EXISTING spawn ownership map (`ownerOf`) for
    `dock_response` - no new "pending request" bookkeeping needed, since the
    requester's own ship spawn already IS the routing key back to them.

    HONEST SCOPE OF "VALIDATION" HERE: the host only checks "do I still
    recognize this as something I exported and that still exists"
    ($XMP.CoopExportedObjectByWireId.{targetId}.exists) - there is no
    economy, no access-control/role model, no ship-registry in this codebase
    yet, so approving a dock has no further game-state consequence beyond the
    confirmation itself. This is deliberately the FOUNDATION PlanMod.md's
    remaining C6 sub-items (repair/resupply, trade, fleet commands) would
    build real stakes on top of, not a complete feature on its own. See
    docs/C6-messprotokoll.md for the full writeup, including the accepted,
    documented risk that a dock_response is not tied to any specific
    dock_request (no pending-request tracking exists), and the pre-existing
    wire-id-collision risk this milestone inherits (not introduces) from every
    earlier milestone's same "reuse the real object's own `.id` as the wire
    id" scheme.

    DELIBERATELY NOT ATTEMPTED IN C6 (same posture as C1-C5's own lists):
      - No undocking detection/handling - there is no docked-ship registry to
        release anything from yet, so there is nothing stateful an undock
        event would need to clean up. Revisit once repair/resupply or trade
        give docking real, persistent state.
      - No pending-request tracking: the server does not remember which
        dock_request a dock_response answers, so a member owning targetId
        could send an unsolicited dock_response for any requesterId it can
        name. Harmless for now (see "honest scope" above) - revisit once
        `approved` has real stakes attached.
      - No object-type check on the host side (a dock_request naming a gate/
        asteroid-field/region's wire id instead of a station's would still be
        "approved" if that id happens to still be tracked) - trusts the real
        game's own physical docking mechanics to have already filtered this
        (a ship cannot physically dock at a gate), same trust posture this
        project already extends to client-side hit detection (A4) and other
        physically-gated local events.
  -->
  <cues>
    <!--
      C2 init: resets both the self-announce guard AND the pending flag
      whenever the Named Pipes API (re)initializes, mirroring
      XMP_Arena_TuningDefaults/PresenceLoop's own reset-on-Reloaded pattern
      (A5). NOTE (correction after an internal review round on the first C2
      draft): agent/src/index.ts's own delivery flag
      (coopSelfAnnounceDeliveredToPipe) is deliberately ONE-SHOT for the
      lifetime of a single WebSocket connection, NOT reset on a pipe
      reconnect - an earlier version of this comment claimed otherwise. What
      DOES need to survive/react to a pipe reconnect is $XMP.CoopSelfAnnounced
      itself (a fresh MD session after a game restart has announced nothing
      yet, regardless of what the agent's own JS-side bookkeeping thinks it
      already delivered once, in a possibly earlier game session).
    -->
    <cue name="XMP_Coop_Init" instantiate="true">
      <conditions>
        <event_cue_signalled cue="md.Named_Pipes.Reloaded" />
      </conditions>
      <actions>
        <do_all>
          <set_value name="$XMP.CoopSelfAnnounced" exact="false" />
          <set_value name="$XMP.CoopPendingSelfAnnounce" exact="false" />
        </do_all>
      </actions>
      <cues>
        <include cue="XMP_Coop_SelfAnnounceCheck" />
      </cues>
    </cue>

    <!--
      Dispatched from XMP_Arena_Dispatch (mod/md/XMP_Arena.xml) whenever a
      `session` message of ANY action is received - mirrors how that same
      dispatch branch already also feeds XMP_Arena_HandleSetaStatus, which
      independently no-ops for actions it doesn't care about. "join" and,
      since C5, "sector_change" are acted on; every other action (leave/
      ready/countdown/seta_on/seta_off) is silently ignored here.

      C5 "Sektorwechsel": a `sector_change` (broadcast by
      XMP_Coop_HandleSectorChange below, whenever ANY session member's own
      sector changes) is treated exactly like someone ELSE'S join for
      sector-mirroring purposes - re-export our own current sector
      (XMP_Coop_ExportSector, unchanged) so the mover's now-empty mirror of
      us gets repopulated. Unlike the "join" branch, this needs NO self/
      other distinction: `sector_change` has no agent-side pipe-loopback
      mechanism the way an explicit-session join does (see this cue's C2
      commentary below) - it only ever reaches this cue via the normal
      server broadcast, which already excludes the sender, so the mover's
      OWN client structurally never receives its own sector_change here.
      The mover's own local teardown (stale mirror + NPC bubble) happens
      synchronously in XMP_Coop_HandleSectorChange itself, not via this
      dispatch path.

      C2 extends this beyond C1's sector export with a SELF/OTHER distinction,
      keyed on the join's playerName versus our own player.name:
        - OUR OWN join, echoed back into the pipe by the agent (C2,
          agent/src/index.ts - an explicit-session join normally goes straight
          from the agent to the relay over the WebSocket and never otherwise
          reaches MD at all): marks $XMP.CoopPendingSelfAnnounce, picked up by
          XMP_Coop_SelfAnnounceCheck below.

          DELIBERATELY does NOT also require player.entity.exists here (fixed
          during an internal review round: the first draft required it in
          THIS condition, which meant a self-join arriving before the
          player's ship exists yet - e.g. still loading a save, exactly the
          same transient condition XMP_Telemetry_BuildAndSend already guards
          against - fell through to the ELSE branch below and wrongly ran
          XMP_Coop_ExportSector instead, then never got another chance to
          announce for the rest of the session, since the agent's own
          delivery is one-shot, see XMP_Coop_Init's comment above). Splitting
          "did our own join arrive" (checked here, unconditionally) from "is
          the player's ship ready to announce" (checked by the poll loop
          below, retried every second until true) is what fixes that.
        - Someone ELSE'S join (the only case C1 handled): export our sector
          (XMP_Coop_ExportSector, unchanged from C1). Our own ship spawn does
          NOT need re-sending here - server/src/sessionManager.ts already
          replays every currently-known spawn (regardless of how it originally
          arrived) to any newly joined member, the same A2 mechanism Arena
          proxies have always relied on.
    -->
    <cue name="XMP_Coop_HandleSessionJoin" instantiate="false">
      <actions>
        <do_all>
          <set_value name="$XMP.ExtractKey" exact="'action'" />
          <include_interrupt_actions ref="XMP_Arena_ExtractField" />
          <set_value name="$XMP.CoopRxAction" exact="$XMP.ExtractValue" />

          <do_if value="$XMP.CoopRxAction == 'join'">
            <do_all>
              <set_value name="$XMP.ExtractKey" exact="'playerName'" />
              <include_interrupt_actions ref="XMP_Arena_ExtractField" />
              <set_value name="$XMP.CoopRxPlayerName" exact="$XMP.ExtractValue" />

              <do_if value="$XMP.CoopRxPlayerName == player.name">
                <do_all>
                  <debug_text text="'[XMultiplayer][Coop] our own join reached the pipe, will announce our ship once ready'" filter="general" />
                  <set_value name="$XMP.CoopPendingSelfAnnounce" exact="true" />
                </do_all>
                <else>
                  <do_all>
                    <debug_text text="'[XMultiplayer][Coop] a session member joined, exporting our current sector'" filter="general" />
                    <include_interrupt_actions ref="XMP_Coop_ExportSector" />
                  </do_all>
                </else>
              </do_if>
            </do_all>
          </do_if>

          <do_if value="$XMP.CoopRxAction == 'sector_change'">
            <do_all>
              <debug_text text="'[XMultiplayer][Coop] a session member changed sector, re-exporting our current sector to them'" filter="general" />
              <include_interrupt_actions ref="XMP_Coop_ExportSector" />
            </do_all>
          </do_if>
        </do_all>
      </actions>
    </cue>

    <!--
      C2: polls once a second (same cadence rationale as
      XMP_Arena_PresenceLoop/SetaCheck - this is a rare, one-time-per-session
      event, no need for telemetry-rate polling) for the condition
      XMP_Coop_HandleSessionJoin above can only PARTIALLY establish by itself:
      our own join has arrived AND the player's ship now actually exists.
      Whichever of the two becomes true last is what actually triggers the
      announce, on whichever poll tick notices both are satisfied.
    -->
    <cue name="XMP_Coop_SelfAnnounceCheck" instantiate="true">
      <delay exact="1s" />
      <actions>
        <do_if value="$XMP.CoopPendingSelfAnnounce and not $XMP.CoopSelfAnnounced and player.entity.exists">
          <do_all>
            <debug_text text="'[XMultiplayer][Coop] player ship ready, announcing our ship now'" filter="general" />
            <include_interrupt_actions ref="XMP_Coop_AnnounceOwnShip" />
            <set_value name="$XMP.CoopSelfAnnounced" exact="true" />
            <set_value name="$XMP.CoopPendingSelfAnnounce" exact="false" />
          </do_all>
        </do_if>
      </actions>
      <cues>
        <cue name="XMP_Coop_SelfAnnounceCheck_Reset" instantiate="true">
          <conditions>
            <cue_completed cue="parent" />
          </conditions>
          <actions>
            <reset_cue cue="parent" />
          </actions>
        </cue>
      </cues>
    </cue>

    <!--
      C5 "Sektorwechsel": detects OUR OWN sector changing (e.g. a gate
      transit) while a Coop session may be active, same polling idiom as
      XMP_Arena_PresenceLoop/SetaLoop (1s cadence - a sector change is a rare,
      not a telemetry-rate event) and runs unconditionally from Reloaded,
      same as XMP_Coop_BubbleTuningDefaults/BubbleCheck (C3) - it relies on
      the SERVER to silently drop anything sent while not actually a member
      of any session (`sessions.sessionCodeOf(clientId)` check,
      server/src/server.ts), rather than gating itself on
      $XMP.CoopSelfAnnounced first. $XMP.CoopLastSectorMacroName seeds to
      `''` (no real sector name is ever the empty string), so the FIRST
      observation after a (re)load is never mistaken for a change - the same
      role XMP_Arena_PresenceLoop's `$XMP.WasInArenaSector = false` seed
      plays for its own boolean comparison, just adapted for a string
      baseline with no natural "false" value.
    -->
    <cue name="XMP_Coop_SectorChangeLoop" instantiate="true">
      <conditions>
        <event_cue_signalled cue="md.Named_Pipes.Reloaded" />
      </conditions>
      <actions>
        <do_all>
          <set_value name="$XMP.CoopLastSectorMacroName" exact="''" />
          <set_value name="$XMP.CoopSectorProxyKnownIds" exact="[]" />
        </do_all>
      </actions>
      <cues>
        <include cue="XMP_Coop_SectorChangeCheck" />
      </cues>
    </cue>

    <cue name="XMP_Coop_SectorChangeCheck" instantiate="true">
      <delay exact="1s" />
      <actions>
        <do_if value="player.entity.exists and player.entity.isclass.ship">
          <do_all>
            <set_value name="$XMP.CoopCurrentSectorMacroName" exact="player.sector.macro.name" />
            <do_if value="$XMP.CoopLastSectorMacroName != '' and $XMP.CoopCurrentSectorMacroName != $XMP.CoopLastSectorMacroName">
              <include_interrupt_actions ref="XMP_Coop_HandleSectorChange" />
            </do_if>
            <set_value name="$XMP.CoopLastSectorMacroName" exact="$XMP.CoopCurrentSectorMacroName" />
          </do_all>
        </do_if>
      </actions>
      <cues>
        <cue name="XMP_Coop_SectorChangeCheck_Reset" instantiate="true">
          <conditions>
            <cue_completed cue="parent" />
          </conditions>
          <actions>
            <reset_cue cue="parent" />
          </actions>
        </cue>
      </cues>
    </cue>

    <!--
      C5 "Sektorwechsel" - the actual teardown, triggered by
      XMP_Coop_SectorChangeCheck the moment it notices OUR OWN sector no
      longer matches its last-seen value. Two independent local cleanups,
      framed deliberately as an explicit "Ladebildschirm-Moment" (PlanMod.md's
      own wording: "kein nahtloser Übergang nötig", no graceful fade needed),
      plus a broadcast so other members refresh their side of the mirror:

        1. Static sector mirror ($XMP.SectorProxies, C1): C1 explicitly
           deferred ever tearing this down ("no despawn/teardown path for
           sector proxies... revisit once C5 exists" - see that file's own
           header). Every currently-held placeholder for the OLD sector is
           destroyed and forgotten (both the keyed tables and the C5-added
           $XMP.CoopSectorProxyKnownIds list are cleared), so
           XMP_Coop_HandleSectorObject's own idempotency guard
           ("not $XMP.SectorProxies.{id}.exists") does not mistakenly treat
           fresh objects for the NEW sector as already-known leftovers from
           the old one (a real risk if two different sectors ever reused the
           same underlying object id, however unlikely).
        2. NPC bubble (C3): PlanMod.md C5 literally says "Bubble abreißen"
           (tear the bubble down) for a sector change, not "let it expire
           naturally" - so every currently-tracked NPC is despawned
           immediately here (reason "sector_change", distinguishable in logs
           from C3's own "left_bubble"), instead of waiting up to
           $XMP.CoopBubbleStaleTimeoutSec (5s) for XMP_Coop_BubbleCheck's own
           stale-timeout pass to notice each one is unreachable in the new
           sector. All three C3/C4 per-id bubble tables
           ($XMP.CoopBubbleKnown/CoopBubbleLastSeenAt/
           CoopBubbleGameObjectByWireId) plus $XMP.CoopBubbleKnownIds itself
           are cleared the same way XMP_Coop_ForgetBubbleNpc (C4) does for a
           single id, just for every currently-tracked id at once - not
           reusing that cue directly since it is written to remove exactly
           ONE id via a rebuild-excluding-that-id pass, not to clear
           everything; a full reset needs no such rebuild.

      Broadcasting `session` `sector_change` (KNOWN, ACCEPTED INACCURACY:
      hardcodes `sessionCode: "arena"`, matching the SAME pre-existing
      limitation XMP_Arena_SetaCheck already has for its own seta_on/seta_off
      broadcasts, A5 - MD never independently learns a Coop session's REAL
      session code, only the agent does, via its explicit "session" CLI flag
      or the XMP_SESSION override, C2's own header explains why. Harmless:
      the server never
      reads `sessionCode` for anything other than the "join" action itself,
      routing every other action by the sender's ALREADY-established session
      membership instead, server/src/server.ts) tells every OTHER session
      member to re-export their own current sector to us
      (XMP_Coop_HandleSessionJoin's new sector_change branch above) - our own
      mirror of THEM would otherwise stay empty in the new sector until that
      arrives.
    -->
    <cue name="XMP_Coop_HandleSectorChange" instantiate="false">
      <actions>
        <do_all>
          <debug_text text="'[XMultiplayer][Coop] sector changed (' + $XMP.CoopLastSectorMacroName + ' -&gt; ' + $XMP.CoopCurrentSectorMacroName + '), tearing down stale mirror'" filter="general" />

          <do_all exact="$XMP.CoopSectorProxyKnownIds" counter="$XMP.CoopSectorTeardownIndex">
            <set_value name="$XMP.CoopSectorTeardownId" exact="$XMP.CoopSectorTeardownIndex.entry" />
            <!-- C6: capture the game id BEFORE destroying the object below, so
                 the reverse lookup (XMP_Coop_HandleSectorObject, used by
                 XMP_Coop_OnDock) can be cleared too - same "capture before
                 destroy" ordering XMP_Arena_DestroyProxy already uses for its
                 own owner-name reverse lookup. -->
            <set_value name="$XMP.CoopSectorTeardownGameId" exact="$XMP.SectorProxies.{$XMP.CoopSectorTeardownId}.id" />
            <destroy_object object="$XMP.SectorProxies.{$XMP.CoopSectorTeardownId}" explosion="false" />
            <set_value name="$XMP.SectorProxies.{$XMP.CoopSectorTeardownId}" exact="null" />
            <set_value name="$XMP.SectorProxyTypes.{$XMP.CoopSectorTeardownId}" exact="null" />
            <set_value name="$XMP.SectorProxyWireIdByGameId.{$XMP.CoopSectorTeardownGameId}" exact="null" />
          </do_all>
          <set_value name="$XMP.CoopSectorProxyKnownIds" exact="[]" />

          <do_all exact="$XMP.CoopBubbleKnownIds" counter="$XMP.CoopSectorBubbleIndex">
            <set_value name="$XMP.CoopSectorBubbleId" exact="$XMP.CoopSectorBubbleIndex.entry" />
            <set_value name="$XMP.Json"
              exact="'{&quot;v&quot;:1,&quot;type&quot;:&quot;despawn&quot;,&quot;seq&quot;:0,&quot;ts&quot;:' + player.age
                + ',&quot;objectId&quot;:&quot;' + $XMP.CoopSectorBubbleId + '&quot;'
                + ',&quot;reason&quot;:&quot;sector_change&quot;'
                + '}'" />
            <include_interrupt_actions ref="XMP_Telemetry_WriteToPipe" />
            <set_value name="$XMP.CoopBubbleKnown.{$XMP.CoopSectorBubbleId}" exact="null" />
            <set_value name="$XMP.CoopBubbleLastSeenAt.{$XMP.CoopSectorBubbleId}" exact="null" />
            <set_value name="$XMP.CoopBubbleGameObjectByWireId.{$XMP.CoopSectorBubbleId}" exact="null" />
          </do_all>
          <set_value name="$XMP.CoopBubbleKnownIds" exact="[]" />

          <set_value name="$XMP.Json"
            exact="'{&quot;v&quot;:1,&quot;type&quot;:&quot;session&quot;,&quot;action&quot;:&quot;sector_change&quot;,&quot;seq&quot;:0,&quot;ts&quot;:' + player.age
              + ',&quot;sessionCode&quot;:&quot;arena&quot;'
              + ',&quot;playerName&quot;:&quot;' + player.name + '&quot;'
              + '}'" />
          <include_interrupt_actions ref="XMP_Telemetry_WriteToPipe" />
        </do_all>
      </actions>
    </cue>

    <!--
      C2 "Host-Schiff als dynamisches Objekt": announces our own REAL ship the
      same way XMP_Arena_OnEnterSector does for Arena-sector presence (same
      field construction, same assumed accessors - player.entity.id/
      macro.name/hullmax/shieldmax, no new risk introduced here), but
      triggered by our own join reaching the pipe (see
      XMP_Coop_HandleSessionJoin above) instead of Arena-sector entry. Once
      sent, this exact `spawn` is what makes XMP_Arena_HandleSpawn (reused
      as-is, unchanged since A2/A3/A4) build a Dead-Reckoning-driven,
      server-authoritative-HP proxy of our ship on every OTHER session
      member's side - literally the "Arena-Code wiederverwendet" PlanMod.md
      calls for.
    -->
    <cue name="XMP_Coop_AnnounceOwnShip" instantiate="false">
      <actions>
        <do_all>
          <set_value name="$XMP.Json"
            exact="'{&quot;v&quot;:1,&quot;type&quot;:&quot;spawn&quot;,&quot;seq&quot;:0,&quot;ts&quot;:' + player.age
              + ',&quot;objectId&quot;:&quot;' + player.entity.id + '&quot;'
              + ',&quot;shipType&quot;:&quot;' + player.entity.macro.name + '&quot;'
              + ',&quot;owner&quot;:&quot;' + player.name + '&quot;'
              + ',&quot;maxHull&quot;:' + player.entity.hullmax
              + ',&quot;maxShield&quot;:' + player.entity.shieldmax
              + '}'" />
          <include_interrupt_actions ref="XMP_Telemetry_WriteToPipe" />
        </do_all>
      </actions>
    </cue>

    <!--
      Host-side (symmetric, see file header): walks player.sector for every
      station/gate/asteroid field/region it can find and exports each as a
      `sector_object`, bracketed by `sector_mirror` begin (with the total
      count, so the receiving end can detect a short transfer) and end.
      objectId is the underlying game object's own `.id` - reused as the wire
      id exactly like XMP_Arena_OnEnterSector reuses player.entity.id for
      spawn.objectId, so an object that is BOTH separately spawned some other
      way (not the case in C1, but future-proofing the id scheme costs
      nothing) would still collide consistently rather than silently alias.
    -->
    <cue name="XMP_Coop_ExportSector" instantiate="false">
      <actions>
        <do_all>
          <set_value name="$XMP.CoopExportObjects" exact="[]" />

          <!-- Data-driven instead of four literal copies of the same three
               lines (code-review feedback on the C1 draft): find_object's
               `class` value and this protocol's `objectType` string happen to
               differ only for asteroid fields ('asteroid' vs 'asteroidfield'),
               so both are carried side by side rather than assuming one can
               be derived from the other. Adding a fifth object class later is
               a one-line addition to this list, not a new copy-pasted block. -->
          <set_value name="$XMP.CoopClasses" exact="[
              table[class='station', objectType='station'],
              table[class='gate', objectType='gate'],
              table[class='asteroid', objectType='asteroidfield'],
              table[class='region', objectType='region']
            ]" />
          <do_all exact="$XMP.CoopClasses" counter="$XMP.CoopClassIndex">
            <set_value name="$XMP.CoopFindClass" exact="$XMP.CoopClassIndex.entry.class" />
            <set_value name="$XMP.CoopFindObjectType" exact="$XMP.CoopClassIndex.entry.objectType" />
            <include_interrupt_actions ref="XMP_Coop_FindObjectsOfClass" />
          </do_all>

          <set_value name="$XMP.Json"
            exact="'{&quot;v&quot;:1,&quot;type&quot;:&quot;sector_mirror&quot;,&quot;action&quot;:&quot;begin&quot;,&quot;seq&quot;:0,&quot;ts&quot;:' + player.age
              + ',&quot;objectCount&quot;:' + $XMP.CoopExportObjects.count
              + '}'" />
          <include_interrupt_actions ref="XMP_Telemetry_WriteToPipe" />

          <do_all exact="$XMP.CoopExportObjects" counter="$XMP.CoopExportIndex">
            <set_value name="$XMP.CoopExportEntry" exact="$XMP.CoopExportIndex.entry" />
            <set_value name="$XMP.Json"
              exact="'{&quot;v&quot;:1,&quot;type&quot;:&quot;sector_object&quot;,&quot;seq&quot;:0,&quot;ts&quot;:' + player.age
                + ',&quot;objectId&quot;:&quot;' + $XMP.CoopExportEntry.id + '&quot;'
                + ',&quot;objectType&quot;:&quot;' + $XMP.CoopExportEntry.objectType + '&quot;'
                + ',&quot;macroName&quot;:&quot;' + $XMP.CoopExportEntry.macroName + '&quot;'
                + ',&quot;position&quot;:{&quot;x&quot;:' + $XMP.CoopExportEntry.posX + ',&quot;y&quot;:' + $XMP.CoopExportEntry.posY + ',&quot;z&quot;:' + $XMP.CoopExportEntry.posZ + '}'
                + ',&quot;rotation&quot;:{&quot;qx&quot;:' + $XMP.CoopExportEntry.rotQX + ',&quot;qy&quot;:' + $XMP.CoopExportEntry.rotQY + ',&quot;qz&quot;:' + $XMP.CoopExportEntry.rotQZ + ',&quot;qw&quot;:' + $XMP.CoopExportEntry.rotQW + '}'
                + '}'" />
            <include_interrupt_actions ref="XMP_Telemetry_WriteToPipe" />
          </do_all>

          <set_value name="$XMP.Json"
            exact="'{&quot;v&quot;:1,&quot;type&quot;:&quot;sector_mirror&quot;,&quot;action&quot;:&quot;end&quot;,&quot;seq&quot;:0,&quot;ts&quot;:' + player.age
              + '}'" />
          <include_interrupt_actions ref="XMP_Telemetry_WriteToPipe" />
          <debug_text text="'[XMultiplayer][Coop] exported ' + $XMP.CoopExportObjects.count + ' sector object(s) from ' + player.sector.macro.name" filter="general" />
        </do_all>
      </actions>
    </cue>

    <!--
      Single enumeration primitive reused for all four object classes (see file
      header, the single highest-risk assumption here): finds every object of
      $XMP.CoopFindClass in player.sector and appends one entry per match to
      $XMP.CoopExportObjects (built up across the four calls in
      XMP_Coop_ExportSector above, then read back out and cleared to []
      there). Each appended entry carries exactly what the outgoing
      sector_object JSON needs, read once here rather than re-reading
      $XMP.CoopIndex.entry's properties again later (by the time
      XMP_Coop_ExportSector's own do_all runs, $XMP.CoopIndex no longer exists
      - it is this cue's own loop variable).
    -->
    <cue name="XMP_Coop_FindObjectsOfClass" instantiate="false">
      <actions>
        <do_all>
          <find_object name="$XMP.CoopFound" class="$XMP.CoopFindClass" sector="player.sector" multiple="true" />
          <do_all exact="$XMP.CoopFound" counter="$XMP.CoopIndex">
            <append_to_list name="$XMP.CoopExportObjects" exact="table[
                id=$XMP.CoopIndex.entry.id,
                objectType=$XMP.CoopFindObjectType,
                macroName=$XMP.CoopIndex.entry.macro.name,
                posX=$XMP.CoopIndex.entry.position.x,
                posY=$XMP.CoopIndex.entry.position.y,
                posZ=$XMP.CoopIndex.entry.position.z,
                rotQX=$XMP.CoopIndex.entry.quaternion.x,
                rotQY=$XMP.CoopIndex.entry.quaternion.y,
                rotQZ=$XMP.CoopIndex.entry.quaternion.z,
                rotQW=$XMP.CoopIndex.entry.quaternion.w
              ]" />
            <!-- C6 "Kommando-Relay": C1 never kept a persistent reference to
                 the REAL object behind an exported sector_object - only the
                 transient $XMP.CoopExportObjects entry above, rebuilt from
                 scratch on every export and read once by
                 XMP_Coop_ExportSector before being discarded. Docking needs
                 to look the real station back up LATER, when a dock_request
                 arrives for its wire id (XMP_Coop_HandleDockRequest below),
                 so a persistent host-side table is added here - the exact
                 same gap C4 already closed for NPCs
                 ($XMP.CoopBubbleGameObjectByWireId), just for exported
                 sector_object entries instead of bubble-tracked NPCs. Keyed
                 by $XMP.CoopIndex.entry.id, which IS the wire objectId (C1
                 reuses the real object's own `.id` directly as
                 sector_object.objectId, unchanged since C1). -->
            <set_value name="$XMP.CoopExportedObjectByWireId.{$XMP.CoopIndex.entry.id}" exact="$XMP.CoopIndex.entry" />
          </do_all>
        </do_all>
      </actions>
    </cue>

    <!--
      Dispatched from XMP_Arena_Dispatch on type == 'sector_mirror'. Purely
      informational bookkeeping: "begin" remembers the promised objectCount and
      resets a received-counter to 0; "end" compares the two and logs a
      mismatch instead of silently pretending the transfer was complete (no
      retry/resume mechanism exists for C1 - see file header). Neither action
      creates or removes any proxy itself.
    -->
    <cue name="XMP_Coop_HandleSectorMirror" instantiate="false">
      <actions>
        <do_all>
          <set_value name="$XMP.ExtractKey" exact="'action'" />
          <include_interrupt_actions ref="XMP_Arena_ExtractField" />
          <set_value name="$XMP.CoopMirrorAction" exact="$XMP.ExtractValue" />

          <do_if value="$XMP.CoopMirrorAction == 'begin'">
            <do_all>
              <set_value name="$XMP.ExtractKey" exact="'objectCount'" />
              <include_interrupt_actions ref="XMP_Arena_ExtractField" />
              <set_value name="$XMP.CoopExpectedObjectCount" exact="$XMP.ExtractValue.{tonumber}" />
              <set_value name="$XMP.CoopReceivedObjectCount" exact="0" />
              <debug_text text="'[XMultiplayer][Coop] sector mirror starting, expecting ' + $XMP.CoopExpectedObjectCount + ' object(s)'" filter="general" />
            </do_all>
          </do_if>
          <do_if value="$XMP.CoopMirrorAction == 'end'">
            <do_all>
              <do_if value="$XMP.CoopReceivedObjectCount == $XMP.CoopExpectedObjectCount">
                <debug_text text="'[XMultiplayer][Coop] sector mirror complete: ' + $XMP.CoopReceivedObjectCount + ' object(s)'" filter="general" />
                <else>
                  <debug_text text="'[XMultiplayer][Coop] sector mirror ended with a MISMATCH: received ' + $XMP.CoopReceivedObjectCount + ' of ' + $XMP.CoopExpectedObjectCount + ' expected object(s)'" filter="general" />
                </else>
              </do_if>
            </do_all>
          </do_if>
        </do_all>
      </actions>
    </cue>

    <!--
      Guest-side (symmetric, see file header): creates a static placeholder for
      one sector_object, idempotently keyed by objectId exactly like
      XMP_Arena_HandleSpawn keys $XMP.Proxies - a re-received object (e.g. a
      later member's join re-triggering the sender's export, see file header
      "no re-export throttling") is a silent no-op, not a duplicate. Kept in a
      SEPARATE table ($XMP.SectorProxies) from $XMP.Proxies on purpose: nothing
      in XMP_Arena_DestroyProxy, the AI-script attach/detach logic, or A4's
      hp_state/hit-detection handling should ever iterate or touch a static
      sector object, and vice versa - keeping the tables distinct makes that
      true by construction rather than by convention.

      Uses ONE placeholder macro for every objectType (see file header
      "guest-side proxies are NOT differentiated by objectType yet") -
      objectType/macroName are still extracted and stored on the entry for
      logging and future per-type handling, just not acted on differently yet.
    -->
    <cue name="XMP_Coop_HandleSectorObject" instantiate="false">
      <actions>
        <do_all>
          <set_value name="$XMP.ExtractKey" exact="'objectId'" />
          <include_interrupt_actions ref="XMP_Arena_ExtractField" />
          <set_value name="$XMP.CoopRxObjectId" exact="$XMP.ExtractValue" />

          <do_if value="not $XMP.SectorProxies.{$XMP.CoopRxObjectId}.exists">
            <do_all>
              <set_value name="$XMP.ExtractKey" exact="'objectType'" />
              <include_interrupt_actions ref="XMP_Arena_ExtractField" />
              <set_value name="$XMP.CoopRxObjectType" exact="$XMP.ExtractValue" />

              <set_value name="$XMP.ExtractKey" exact="'position'" />
              <include_interrupt_actions ref="XMP_Arena_ExtractField" />
              <set_value name="$XMP.CoopRxPositionJson" exact="$XMP.ExtractValue" />

              <set_value name="$XMP.ExtractKey" exact="'rotation'" />
              <include_interrupt_actions ref="XMP_Arena_ExtractField" />
              <set_value name="$XMP.CoopRxRotationJson" exact="$XMP.ExtractValue" />

              <set_value name="$XMP.RxLine" exact="$XMP.CoopRxPositionJson" />
              <set_value name="$XMP.ExtractKey" exact="'x'" />
              <include_interrupt_actions ref="XMP_Arena_ExtractField" />
              <set_value name="$XMP.CoopRxPosX" exact="$XMP.ExtractValue.{tonumber}" />
              <set_value name="$XMP.ExtractKey" exact="'y'" />
              <include_interrupt_actions ref="XMP_Arena_ExtractField" />
              <set_value name="$XMP.CoopRxPosY" exact="$XMP.ExtractValue.{tonumber}" />
              <set_value name="$XMP.ExtractKey" exact="'z'" />
              <include_interrupt_actions ref="XMP_Arena_ExtractField" />
              <set_value name="$XMP.CoopRxPosZ" exact="$XMP.ExtractValue.{tonumber}" />

              <set_value name="$XMP.RxLine" exact="$XMP.CoopRxRotationJson" />
              <set_value name="$XMP.ExtractKey" exact="'qx'" />
              <include_interrupt_actions ref="XMP_Arena_ExtractField" />
              <set_value name="$XMP.CoopRxRotQX" exact="$XMP.ExtractValue.{tonumber}" />
              <set_value name="$XMP.ExtractKey" exact="'qy'" />
              <include_interrupt_actions ref="XMP_Arena_ExtractField" />
              <set_value name="$XMP.CoopRxRotQY" exact="$XMP.ExtractValue.{tonumber}" />
              <set_value name="$XMP.ExtractKey" exact="'qz'" />
              <include_interrupt_actions ref="XMP_Arena_ExtractField" />
              <set_value name="$XMP.CoopRxRotQZ" exact="$XMP.ExtractValue.{tonumber}" />
              <set_value name="$XMP.ExtractKey" exact="'qw'" />
              <include_interrupt_actions ref="XMP_Arena_ExtractField" />
              <set_value name="$XMP.CoopRxRotQW" exact="$XMP.ExtractValue.{tonumber}" />

              <!-- Placeholder macro name - see file header
                   "$XMP.CoopPlaceholderMacro". Must be replaced with a
                   real, confirmed macro name before this can run in-game. -->
              <set_value name="$XMP.CoopPlaceholderMacro" exact="'xmp_coop_placeholder_macro'" />

              <create_station name="$XMP.NewSectorProxy" macro="$XMP.CoopPlaceholderMacro" sector="player.sector"
                position="table[x=$XMP.CoopRxPosX, y=$XMP.CoopRxPosY, z=$XMP.CoopRxPosZ]"
                rotation="table[x=$XMP.CoopRxRotQX, y=$XMP.CoopRxRotQY, z=$XMP.CoopRxRotQZ, w=$XMP.CoopRxRotQW]"
                owner="ownerless" />
              <set_value name="$XMP.SectorProxies.{$XMP.CoopRxObjectId}" exact="$XMP.NewSectorProxy" />
              <set_value name="$XMP.SectorProxyTypes.{$XMP.CoopRxObjectId}" exact="$XMP.CoopRxObjectType" />
              <!-- C6 "Kommando-Relay": reverse lookup (game object -> wire
                   objectId), needed by XMP_Coop_OnDock below - a docking
                   event exposes the game object docked at, not our own wire
                   id for it. Mirrors $XMP.ProxyWireIdByGameId's existing role
                   for ship proxies (A4) exactly, just for sector proxies. -->
              <set_value name="$XMP.SectorProxyWireIdByGameId.{$XMP.NewSectorProxy.id}" exact="$XMP.CoopRxObjectId" />
              <!-- C5 "Sektorwechsel": C1 tracked $XMP.SectorProxies only as a
                   keyed existence table (no iterable list), since C1 itself
                   never needed to walk "every currently mirrored sector
                   object" - only ever look one up by a known id. C5 does need
                   that (tearing all of them down on our OWN sector change,
                   XMP_Coop_HandleSectorChange below), so this list is now
                   built alongside the keyed table, same "keep a parallel
                   list next to the keyed table" idiom C3 already established
                   for $XMP.CoopBubbleKnownIds. -->
              <append_to_list name="$XMP.CoopSectorProxyKnownIds" exact="$XMP.CoopRxObjectId" />
              <set_value name="$XMP.CoopReceivedObjectCount" exact="$XMP.CoopReceivedObjectCount + 1" />
              <debug_text text="'[XMultiplayer][Coop] mirrored ' + $XMP.CoopRxObjectType + ' objectId=' + $XMP.CoopRxObjectId" filter="general" />
            </do_all>
            <else>
              <debug_text text="'[XMultiplayer][Coop] sector_object for already-known objectId=' + $XMP.CoopRxObjectId + ', ignoring'" filter="general" />
            </else>
          </do_if>
        </do_all>
      </actions>
    </cue>

    <!--
      C6 "Kommando-Relay": guest-side local docking detection. Fires (new,
      unconfirmed assumption, same posture as every other event condition in
      this project) whenever the LOCAL player's own ship docks at ANYTHING -
      filtered via $XMP.SectorProxyWireIdByGameId (populated above) to only
      react when the docked-at object is a KNOWN mirrored sector proxy;
      docking at a real, local, non-mirrored station is completely ordinary
      single-player X4 behavior this mod has no business intercepting.

      ASSUMPTION: an `event_object_docked` condition exists, scoped to
      `player.entity` (mirrors `event_object_fired`'s existing scoping, A4),
      exposing the docked-at object as `event.object` (mirrors
      `event_object_attacked`'s `event.object`, A4). If this event name/shape
      is wrong, only this one cue needs to change - the reverse lookup and
      the rest of the request/response flow do not depend on how the docking
      attempt is detected.

      PlanMod.md C6's own parenthetical ("Ohne Relay: rein physisches
      Andocken am Proxy möglich, aber ohne Konsequenzen"): this cue does not
      gate or prevent the local docking itself - by the time it fires, the
      player has ALREADY physically docked (nothing server-side has any say
      over local physics). What this adds is the report-and-confirm round
      trip that turns an already-happening, consequence-free local
      interaction into a validated, acknowledged one.
    -->
    <cue name="XMP_Coop_OnDock" instantiate="true">
      <conditions>
        <event_object_docked object="player.entity" />
      </conditions>
      <actions>
        <do_if value="player.entity.exists and player.entity.isclass.ship and $XMP.SectorProxyWireIdByGameId.{event.object.id}.exists">
          <do_all>
            <set_value name="$XMP.Json"
              exact="'{&quot;v&quot;:1,&quot;type&quot;:&quot;dock_request&quot;,&quot;seq&quot;:0,&quot;ts&quot;:' + player.age
                + ',&quot;targetId&quot;:&quot;' + $XMP.SectorProxyWireIdByGameId.{event.object.id} + '&quot;'
                + ',&quot;requesterId&quot;:&quot;' + player.entity.id + '&quot;'
                + '}'" />
            <include_interrupt_actions ref="XMP_Telemetry_WriteToPipe" />
            <debug_text text="'[XMultiplayer][Coop] docking at mirrored station (objectId=' + $XMP.SectorProxyWireIdByGameId.{event.object.id} + '), requesting host confirmation'" filter="general" />
          </do_all>
        </do_if>
      </actions>
      <cues>
        <cue name="XMP_Coop_OnDock_Reset" instantiate="true">
          <conditions>
            <cue_completed cue="parent" />
          </conditions>
          <actions>
            <reset_cue cue="parent" />
          </actions>
        </cue>
      </cues>
    </cue>

    <!--
      C6, host-side: validates an incoming dock_request against our own
      persistent record of what we've actually exported
      ($XMP.CoopExportedObjectByWireId, populated in
      XMP_Coop_FindObjectsOfClass above) and responds.

      HONEST LIMIT OF "VALIDATION" HERE: only "do we still recognize this as
      something we exported and that still exists" - there is no economy, no
      access-control/role model, no ship-registry to check against yet
      (PlanMod.md's remaining C6 sub-items - repair/resupply, trade, fleet
      commands - are what would eventually give a dock real stakes;
      explicitly out of scope for this milestone, see
      docs/C6-messprotokoll.md). Approving today has no further game-state
      consequence beyond sending the response itself.

      Builds two full, separate JSON strings (approved vs. denied) rather
      than conditionally omitting `reason` from one shared string - this
      codebase has no established pattern anywhere for building a JSON
      string with a conditionally-present field (every existing sender
      either always includes a field or never does), so branching into two
      complete literals is the same idiom already used for e.g.
      XMP_Arena_SetaCheck's seta_on/seta_off pair.
    -->
    <cue name="XMP_Coop_HandleDockRequest" instantiate="false">
      <actions>
        <do_all>
          <set_value name="$XMP.ExtractKey" exact="'targetId'" />
          <include_interrupt_actions ref="XMP_Arena_ExtractField" />
          <set_value name="$XMP.CoopDockTargetId" exact="$XMP.ExtractValue" />

          <set_value name="$XMP.ExtractKey" exact="'requesterId'" />
          <include_interrupt_actions ref="XMP_Arena_ExtractField" />
          <set_value name="$XMP.CoopDockRequesterId" exact="$XMP.ExtractValue" />

          <do_if value="$XMP.CoopExportedObjectByWireId.{$XMP.CoopDockTargetId}.exists">
            <do_all>
              <set_value name="$XMP.Json"
                exact="'{&quot;v&quot;:1,&quot;type&quot;:&quot;dock_response&quot;,&quot;seq&quot;:0,&quot;ts&quot;:' + player.age
                  + ',&quot;targetId&quot;:&quot;' + $XMP.CoopDockTargetId + '&quot;'
                  + ',&quot;requesterId&quot;:&quot;' + $XMP.CoopDockRequesterId + '&quot;'
                  + ',&quot;approved&quot;:true'
                  + '}'" />
              <include_interrupt_actions ref="XMP_Telemetry_WriteToPipe" />
              <debug_text text="'[XMultiplayer][Coop] dock_request approved for targetId=' + $XMP.CoopDockTargetId" filter="general" />
            </do_all>
            <else>
              <do_all>
                <set_value name="$XMP.Json"
                  exact="'{&quot;v&quot;:1,&quot;type&quot;:&quot;dock_response&quot;,&quot;seq&quot;:0,&quot;ts&quot;:' + player.age
                    + ',&quot;targetId&quot;:&quot;' + $XMP.CoopDockTargetId + '&quot;'
                    + ',&quot;requesterId&quot;:&quot;' + $XMP.CoopDockRequesterId + '&quot;'
                    + ',&quot;approved&quot;:false'
                    + ',&quot;reason&quot;:&quot;station no longer known&quot;'
                    + '}'" />
                <include_interrupt_actions ref="XMP_Telemetry_WriteToPipe" />
                <debug_text text="'[XMultiplayer][Coop] dock_request DENIED for targetId=' + $XMP.CoopDockTargetId + ' (not a currently exported station)'" filter="general" />
              </do_all>
            </else>
          </do_if>
        </do_all>
      </actions>
    </cue>

    <!--
      C6, guest-side: reacts to the host's confirmation/denial. The
      requesterId == player.entity.id check is defense in depth, not
      currently reachable in normal operation (the server already routes
      dock_response point-to-point to whoever owns requesterId, server/src/
      server.ts) - same belt-and-suspenders posture as
      XMP_Arena_HandleSetaStatus's own by-name lookup guard.

      "Lokale Abspielung" (PlanMod.md C6): a debug_text notification is the
      full extent of it for V1 - same accepted, undocumented-elsewhere
      "no real player-facing notification mechanism exists yet" limitation
      A5 already flagged for its own kill-feed/SETA notifications
      (docs/A5-messprotokoll.md "Nächste Schritte"), not a new gap C6
      introduces.
    -->
    <cue name="XMP_Coop_HandleDockResponse" instantiate="false">
      <actions>
        <do_all>
          <set_value name="$XMP.ExtractKey" exact="'requesterId'" />
          <include_interrupt_actions ref="XMP_Arena_ExtractField" />
          <set_value name="$XMP.CoopDockRxRequesterId" exact="$XMP.ExtractValue" />

          <do_if value="player.entity.exists and $XMP.CoopDockRxRequesterId == player.entity.id">
            <do_all>
              <set_value name="$XMP.ExtractKey" exact="'approved'" />
              <include_interrupt_actions ref="XMP_Arena_ExtractField" />
              <set_value name="$XMP.CoopDockRxApproved" exact="$XMP.ExtractValue" />

              <do_if value="$XMP.CoopDockRxApproved == 'true'">
                <debug_text text="'[XMultiplayer][Coop] docking confirmed by host'" filter="general" />
                <else>
                  <do_all>
                    <set_value name="$XMP.ExtractKey" exact="'reason'" />
                    <include_interrupt_actions ref="XMP_Arena_ExtractField" />
                    <set_value name="$XMP.CoopDockRxReason" exact="$XMP.ExtractValue" />
                    <debug_text text="'[XMultiplayer][Coop] docking DENIED by host: ' + $XMP.CoopDockRxReason" filter="general" />
                  </do_all>
                </else>
              </do_if>
            </do_all>
          </do_if>
        </do_all>
      </actions>
    </cue>

    <!--
      C3 "NPC-Bubble mit Interest Management": tuning constants, gathered here
      exactly like XMP_Arena_TuningDefaults (A3). All four are PLACEHOLDER
      values, not measured (PlanMod.md explicitly calls for real performance
      measurement before finalizing this milestone's numbers - "Budget
      definieren... Performance-Messung!" - which needs an X4 installation
      this development environment does not have):
        - CoopBubbleRadius: 20000 (20km). Arbitrary "nearby traffic" guess.
        - CoopBubbleMaxObjects: 10, matching MAX_NPC_SPAWNS_PER_CLIENT
          (protocol/src/limits.ts) EXACTLY on purpose - if this ever drifts
          out of sync, the server would silently reject NPC spawns beyond its
          own cap after MD already did the work of finding and building them,
          wasted effort for no gain. Also matches PlanMod's own "~10
          nächsten/relevantesten Objekte" number.
        - CoopBubbleTickIntervalSec: 2. ONE unified rate for every tracked
          NPC, not PlanMod's literal two-tier "volle Rate für nahe, seltener
          für ferne" design - deliberate V1 simplification (see this file's
          "DELIBERATELY NOT ATTEMPTED IN C3" note in the header). The nearest-
          N cutoff (CoopBubbleMaxObjects, combined with find_object's assumed
          distance-sorted results) already captures "relevanteste Objekte"
          without needing a second, slower tier - Dead Reckoning (A3) is
          exactly the mechanism meant to paper over a coarser update cadence
          than the player's own 10Hz telemetry via extrapolation, and this
          rate is well inside what its existing tuning already tolerates
          (compare $XMP.DeadReckoningUpdateTimeoutSec = 5s, XMP_Arena.xml).
        - CoopBubbleStaleTimeoutSec: 5 (2.5x the tick interval, same slack
          rationale as the Dead Reckoning update timeout it happens to match).
    -->
    <cue name="XMP_Coop_BubbleTuningDefaults" instantiate="true">
      <conditions>
        <event_cue_signalled cue="md.Named_Pipes.Reloaded" />
      </conditions>
      <actions>
        <do_all>
          <set_value name="$XMP.CoopBubbleRadius" exact="20000" />
          <set_value name="$XMP.CoopBubbleMaxObjects" exact="10" />
          <set_value name="$XMP.CoopBubbleTickIntervalSec" exact="2" />
          <set_value name="$XMP.CoopBubbleStaleTimeoutSec" exact="5" />
          <set_value name="$XMP.CoopBubbleKnownIds" exact="[]" />
        </do_all>
      </actions>
      <cues>
        <include cue="XMP_Coop_BubbleCheck" />
      </cues>
    </cue>

    <!--
      C3 main loop. THE SECOND HIGHEST-RISK ITEM IN THIS FILE, after
      XMP_Coop_FindObjectsOfClass (C1): extends the ALREADY-assumed
      `find_object` action (C1: `class`/`sector`/`multiple`) with THREE
      further assumed attributes it has never needed before -
      `position`/`maxdist` (a radius filter around a reference point) and
      `sortbydistanceto` (pre-sorted, nearest-first results) - plus a NEW
      assumed `class` value, `'ship'`, alongside C1's `'station'`/`'gate'`/
      `'asteroid'`/`'region'`. If any of these three new attributes don't
      exist as assumed, the fallback is the same as C1's: a more specific
      per-purpose action (e.g. a `find_ship`-style action with its own
      distance/sort parameters, per various community MD references) - only
      this one cue's `find_object` call would need to change.

      Bubble center is OUR OWN ship's position, not the other session
      member's (a deliberate C2-consistent simplification - see this file's
      "DELIBERATELY NOT ATTEMPTED IN C3" header note for the full reasoning).

      Excludes (a) our own ship and (b) anything already tracked as a proxy
      of another session member ($XMP.ProxyWireIdByGameId, populated by
      XMP_Arena_HandleSpawn, A2) - without (b), a remote player's OWN proxy
      ship (a real local game object on THIS client, invulnerable and
      AI-piloted, but real nonetheless) would get re-exported as if it were
      an NPC, which the receiving side (that SAME remote player, or a third
      member) would then mirror as a proxy-of-a-proxy: nonsensical, and for
      the remote player themselves, a proxy of their OWN ship.

      Budget enforcement is MANUAL (a plain counter compared each iteration),
      not a further assumed `find_object` parameter (e.g. a hypothetical
      `resultmaxcount`) - deliberately, to avoid stacking a FOURTH new
      engine-side assumption on top of the three above for something a
      simple do_all/counter comparison (an already-used, low-risk pattern)
      achieves just as well.

      The spawn/state_update/despawn JSON string-building below duplicates
      the same pattern already written out in XMP_Arena_OnEnterSector/
      XMP_Telemetry_BuildAndSend/XMP_Coop_ExportSector rather than sharing it
      - not an oversight, MD's `include_interrupt_actions` has no confirmed
      way to pass parameters into a shared cue and read a return value back
      out (unlike XMP_Arena_ExtractField, which is shared successfully
      because it only needs $XMP.-namespaced globals in and out, no return
      value), so each JSON-building site stays its own literal block.

      Lifecycle (spawn/despawn on bubble entry/exit) uses a "last seen
      timestamp + timeout" design, not a "seen this tick: yes/no" flag reset
      every tick - MD has no confirmed way to bulk-clear a keyed table
      ($XMP.CoopBubbleKnown.{id} for every previously-known id) each tick, so
      resetting per-tick "seen" flags cleanly is not obviously possible.
      Reusing a timestamp instead sidesteps that entirely (nothing needs
      clearing, staleness is just "how long since this was last refreshed",
      the exact same idiom aiscripts/XMP.ProxyPilot.xml's Dead Reckoning
      already uses for its own update timeout) - newly-found objects refresh
      their own timestamp and get appended into $XMP.CoopBubbleKnownIds
      (a plain list, C1's already-used "build fresh, don't mutate in place"
      idiom) during the SAME pass that later reads that same list for
      despawn detection; this is safe (not a stale-read race) specifically
      BECAUSE staleness is timestamp-based, not iteration-order-based - a
      just-appended entry's timestamp is always "now", so it always reads as
      fresh regardless of when in this tick it was appended relative to the
      despawn pass.
    -->
    <cue name="XMP_Coop_BubbleCheck" instantiate="true">
      <delay exact="$XMP.CoopBubbleTickIntervalSec" />
      <actions>
        <do_if value="player.entity.exists and player.entity.isclass.ship">
          <do_all>
            <find_object name="$XMP.CoopBubbleFound" class="'ship'" sector="player.sector"
              position="player.entity.position" maxdist="$XMP.CoopBubbleRadius"
              sortbydistanceto="player.entity.position" multiple="true" />

            <set_value name="$XMP.CoopBubbleTrackedCount" exact="0" />
            <do_all exact="$XMP.CoopBubbleFound" counter="$XMP.CoopBubbleFindIndex">
              <set_value name="$XMP.CoopBubbleEntry" exact="$XMP.CoopBubbleFindIndex.entry" />
              <do_if value="$XMP.CoopBubbleTrackedCount &lt; $XMP.CoopBubbleMaxObjects
                  and $XMP.CoopBubbleEntry.id != player.entity.id
                  and not $XMP.ProxyWireIdByGameId.{$XMP.CoopBubbleEntry.id}.exists">
                <do_all>
                  <set_value name="$XMP.CoopBubbleTrackedCount" exact="$XMP.CoopBubbleTrackedCount + 1" />
                  <set_value name="$XMP.CoopBubbleLastSeenAt.{$XMP.CoopBubbleEntry.id}" exact="player.age" />
                  <!-- C4 "Hit-Relay auf NPCs": keeps a live reference to the REAL
                       local game object for this NPC, refreshed every tick it is
                       still in the bubble (both the "already known" and "newly
                       found" branches below need this, so it is set here, before
                       the known/new do_if). XMP_Arena_HandleHpState
                       (mod/md/XMP_Arena.xml) reads this table to apply
                       server-authoritative damage to the ACTUAL NPC object -
                       nothing else in this file kept any object handle around for
                       it before C4 (only its position/rotation/velocity/hull/
                       shield SNAPSHOT fields were read out of $XMP.CoopBubbleEntry
                       and discarded once this loop iteration ended). -->
                  <set_value name="$XMP.CoopBubbleGameObjectByWireId.{$XMP.CoopBubbleEntry.id}" exact="$XMP.CoopBubbleEntry" />

                  <do_if value="not $XMP.CoopBubbleKnown.{$XMP.CoopBubbleEntry.id}.exists">
                    <do_all>
                      <append_to_list name="$XMP.CoopBubbleKnownIds" exact="$XMP.CoopBubbleEntry.id" />
                      <set_value name="$XMP.CoopBubbleKnown.{$XMP.CoopBubbleEntry.id}" exact="true" />
                      <set_value name="$XMP.Json"
                        exact="'{&quot;v&quot;:1,&quot;type&quot;:&quot;spawn&quot;,&quot;seq&quot;:0,&quot;ts&quot;:' + player.age
                          + ',&quot;objectId&quot;:&quot;' + $XMP.CoopBubbleEntry.id + '&quot;'
                          + ',&quot;shipType&quot;:&quot;' + $XMP.CoopBubbleEntry.macro.name + '&quot;'
                          + ',&quot;owner&quot;:&quot;' + player.name + '&quot;'
                          + ',&quot;maxHull&quot;:' + $XMP.CoopBubbleEntry.hullmax
                          + ',&quot;maxShield&quot;:' + $XMP.CoopBubbleEntry.shieldmax
                          + ',&quot;category&quot;:&quot;npc&quot;'
                          + '}'" />
                      <include_interrupt_actions ref="XMP_Telemetry_WriteToPipe" />
                      <debug_text text="'[XMultiplayer][Coop] npc entered bubble: ' + $XMP.CoopBubbleEntry.macro.name + ' (objectId=' + $XMP.CoopBubbleEntry.id + ')'" filter="general" />
                    </do_all>
                    <else>
                      <do_all>
                        <set_value name="$XMP.Json"
                          exact="'{&quot;v&quot;:1,&quot;type&quot;:&quot;state_update&quot;,&quot;seq&quot;:0,&quot;ts&quot;:' + player.age
                            + ',&quot;shipId&quot;:&quot;' + $XMP.CoopBubbleEntry.id + '&quot;'
                            + ',&quot;position&quot;:{&quot;x&quot;:' + $XMP.CoopBubbleEntry.position.x + ',&quot;y&quot;:' + $XMP.CoopBubbleEntry.position.y + ',&quot;z&quot;:' + $XMP.CoopBubbleEntry.position.z + '}'
                            + ',&quot;rotation&quot;:{&quot;qx&quot;:' + $XMP.CoopBubbleEntry.quaternion.x + ',&quot;qy&quot;:' + $XMP.CoopBubbleEntry.quaternion.y + ',&quot;qz&quot;:' + $XMP.CoopBubbleEntry.quaternion.z + ',&quot;qw&quot;:' + $XMP.CoopBubbleEntry.quaternion.w + '}'
                            + ',&quot;velocity&quot;:{&quot;x&quot;:' + $XMP.CoopBubbleEntry.speed.x + ',&quot;y&quot;:' + $XMP.CoopBubbleEntry.speed.y + ',&quot;z&quot;:' + $XMP.CoopBubbleEntry.speed.z + '}'
                            + '}'" />
                        <include_interrupt_actions ref="XMP_Telemetry_WriteToPipe" />
                      </do_all>
                    </else>
                  </do_if>
                </do_all>
              </do_if>
            </do_all>

            <!-- Despawn pass: anything in the PREVIOUS tick's known-ids list
                 not refreshed above (either genuinely gone, out of radius, or
                 budget-bumped by something now closer) is stale and gets
                 despawned; survivors are rebuilt into a fresh list, the same
                 "rebuild, don't mutate in place" idiom as
                 XMP_Coop_ExportSector's $XMP.CoopExportObjects (C1). -->
            <set_value name="$XMP.CoopBubbleSurvivingIds" exact="[]" />
            <do_all exact="$XMP.CoopBubbleKnownIds" counter="$XMP.CoopBubbleDespawnIndex">
              <set_value name="$XMP.CoopBubbleCheckId" exact="$XMP.CoopBubbleDespawnIndex.entry" />
              <do_if value="player.age - $XMP.CoopBubbleLastSeenAt.{$XMP.CoopBubbleCheckId} &gt; $XMP.CoopBubbleStaleTimeoutSec">
                <do_all>
                  <set_value name="$XMP.Json"
                    exact="'{&quot;v&quot;:1,&quot;type&quot;:&quot;despawn&quot;,&quot;seq&quot;:0,&quot;ts&quot;:' + player.age
                      + ',&quot;objectId&quot;:&quot;' + $XMP.CoopBubbleCheckId + '&quot;'
                      + ',&quot;reason&quot;:&quot;left_bubble&quot;'
                      + '}'" />
                  <include_interrupt_actions ref="XMP_Telemetry_WriteToPipe" />
                  <set_value name="$XMP.CoopBubbleKnown.{$XMP.CoopBubbleCheckId}" exact="null" />
                  <!-- Review-round fix: an earlier draft cleared CoopBubbleKnown
                       here but left CoopBubbleLastSeenAt.{id} behind forever,
                       an unbounded (if functionally harmless - overwritten on
                       re-entry) growth of that table over a play session.
                       Cleared here too now, symmetrically. -->
                  <set_value name="$XMP.CoopBubbleLastSeenAt.{$XMP.CoopBubbleCheckId}" exact="null" />
                  <!-- C4: same symmetric-cleanup rationale as the LastSeenAt fix
                       just above, applied to the game-object-reference table this
                       milestone adds - an NPC that simply drifts back out of
                       radius (not destroyed) must not leave a stale reference
                       behind either. -->
                  <set_value name="$XMP.CoopBubbleGameObjectByWireId.{$XMP.CoopBubbleCheckId}" exact="null" />
                  <debug_text text="'[XMultiplayer][Coop] npc left bubble: objectId=' + $XMP.CoopBubbleCheckId" filter="general" />
                </do_all>
                <else>
                  <append_to_list name="$XMP.CoopBubbleSurvivingIds" exact="$XMP.CoopBubbleCheckId" />
                </else>
              </do_if>
            </do_all>
            <set_value name="$XMP.CoopBubbleKnownIds" exact="$XMP.CoopBubbleSurvivingIds" />
          </do_all>
        </do_if>
      </actions>
      <cues>
        <cue name="XMP_Coop_BubbleCheck_Reset" instantiate="true">
          <conditions>
            <cue_completed cue="parent" />
          </conditions>
          <actions>
            <reset_cue cue="parent" />
          </actions>
        </cue>
      </cues>
    </cue>

    <!--
      C4 "Hit-Relay auf NPCs": shared cleanup for a real, locally-tracked NPC
      that was destroyed via server-authoritative hp_state
      (mod/md/XMP_Arena.xml's XMP_Arena_HandleHpState's new third branch),
      called OUTSIDE XMP_Coop_BubbleCheck's own tick loop - unlike that cue's
      inline stale-timeout cleanup above (which rebuilds
      $XMP.CoopBubbleKnownIds as part of ALREADY iterating it), this is a
      one-off removal triggered by combat, not by the next bubble tick
      noticing the object is gone on its own. Caller sets
      $XMP.CoopForgetObjectId before including this cue. Rebuilds
      $XMP.CoopBubbleKnownIds excluding that id (same "rebuild, don't mutate
      in place" idiom as the stale-timeout pass above and C1's
      $XMP.CoopExportObjects), so a real kill does not leave a stale entry
      that the NEXT BubbleCheck tick's stale-timeout pass would otherwise
      (harmlessly, but redundantly) also try to despawn minutes later with the
      misleading reason "left_bubble" for an object that was actually
      destroyed, not merely out of radius.
    -->
    <cue name="XMP_Coop_ForgetBubbleNpc" instantiate="false">
      <actions>
        <do_all>
          <set_value name="$XMP.CoopForgetSurvivingIds" exact="[]" />
          <do_all exact="$XMP.CoopBubbleKnownIds" counter="$XMP.CoopForgetIndex">
            <do_if value="$XMP.CoopForgetIndex.entry != $XMP.CoopForgetObjectId">
              <append_to_list name="$XMP.CoopForgetSurvivingIds" exact="$XMP.CoopForgetIndex.entry" />
            </do_if>
          </do_all>
          <set_value name="$XMP.CoopBubbleKnownIds" exact="$XMP.CoopForgetSurvivingIds" />
          <set_value name="$XMP.CoopBubbleKnown.{$XMP.CoopForgetObjectId}" exact="null" />
          <set_value name="$XMP.CoopBubbleLastSeenAt.{$XMP.CoopForgetObjectId}" exact="null" />
          <set_value name="$XMP.CoopBubbleGameObjectByWireId.{$XMP.CoopForgetObjectId}" exact="null" />
        </do_all>
      </actions>
    </cue>
  </cues>
</mdscript>
